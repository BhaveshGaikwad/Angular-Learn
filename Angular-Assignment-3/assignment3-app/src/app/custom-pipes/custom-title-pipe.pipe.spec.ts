import { CustomTitlePipePipe } from './custom-title-pipe.pipe';

describe('CustomTitlePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomTitlePipePipe();
    expect(pipe).toBeTruthy();
  });
});
